version: 1

metadata:
  name: authentication

service:
  name: authentication
  exec: authentication_server

steps:
  - id: ensure-user-group
    type: ensure_user_group
    user: globular
    group: globular
    home: "{{.StateDir}}"
    shell: /usr/sbin/nologin
    system: true

  - id: ensure-dirs
    type: ensure_dirs
    dirs:
      - path: "{{.Prefix}}"
        owner: root
        group: root
        mode: 0755
      - path: "{{.Prefix}}/bin"
        owner: root
        group: root
        mode: 0755

      - path: "{{.StateDir}}"
        owner: globular
        group: globular
        mode: 0750
      - path: "{{.StateDir}}/authentication"
        owner: globular
        group: globular
        mode: 0750

      - path: "{{.ConfigDir}}"
        owner: root
        group: root
        mode: 0755
      - path: "{{.ConfigDir}}/authentication"
        owner: root
        group: root
        mode: 0755

  - id: install-authentication-config
    type: install_files
    files:
      - path: "{{.ConfigDir}}/authentication/config.json"
        owner: root
        group: root
        mode: 0644
        atomic: true
        # NOTE: this is intended to be replaced by node-agent/controller later.
        # It is seeded from 'authentication_server --describe'.
        content: |


  - id: install-authentication-service
    type: install_services
    units:
      - name: globular-authentication.service
        owner: root
        group: root
        mode: 0644
        atomic: true
        content: |
          [Unit]
          Description=Globular authentication
          After=network-online.target
          Wants=network-online.target

          [Service]
          Type=simple
          User=globular
          Group=globular
          WorkingDirectory={{.StateDir}}/authentication
          ExecStart={{.Prefix}}/bin/authentication_server
          Restart=on-failure
          RestartSec=2
          LimitNOFILE=524288

          [Install]
          WantedBy=multi-user.target

  - id: enable-authentication
    type: enable_services
    services:
      - globular-authentication.service

  - id: start-authentication
    type: start_services
    services:
      - globular-authentication.service
    restart_on_files:
      globular-authentication.service:
        - "{{.ConfigDir}}/authentication/config.json"
    binaries:
      globular-authentication.service: authentication_server

  - id: health-authentication
    type: health_checks
    services:
      - globular-authentication.service
