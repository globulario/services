version: 1

metadata:
  name: catalog

service:
  name: catalog
  exec: catalog_server

steps:
  - id: ensure-user-group
    type: ensure_user_group
    user: globular
    group: globular
    home: "{{.StateDir}}"
    shell: /usr/sbin/nologin
    system: true

  - id: ensure-dirs
    type: ensure_dirs
    dirs:
      - path: "{{.Prefix}}"
        owner: root
        group: root
        mode: 0755
      - path: "{{.Prefix}}/bin"
        owner: root
        group: root
        mode: 0755

      - path: "{{.StateDir}}"
        owner: globular
        group: globular
        mode: 0750
      - path: "{{.StateDir}}/services"
        owner: globular
        group: globular
        mode: 0750
      - path: "{{.StateDir}}/catalog"
        owner: globular
        group: globular
        mode: 0750

      # TLS directories (certificates must be provisioned by Day-0 bootstrap)
      - path: "{{.StateDir}}/pki"
        owner: globular
        group: globular
        mode: 0750
      # INV-PKI-1: Removed obsolete config/tls directory - all certs under pki/

  - id: install-catalog-payload
    type: install_package_payload
    install_bins: true
    install_config: false
    install_spec: false
    install_systemd: false

  - id: ensure-catalog-config
    type: ensure_service_config
    service_name: catalog
    exec: catalog_server
    address_host: auto
    rewrite_if_out_of_range: true

  - id: install-catalog-service
    type: install_services
    units:
      - name: globular-catalog.service
        owner: root
        group: root
        mode: 0644
        atomic: true
        content: |
          [Unit]
          Description=Globular catalog
          After=network-online.target globular-event.service globular-persistence.service globular-resource.service
          Wants=network-online.target globular-event.service globular-persistence.service globular-resource.service

          [Service]
          Type=simple
          User=globular
          Group=globular
          WorkingDirectory={{.StateDir}}/catalog
          Environment=GLOBULAR_SERVICES_DIR={{.StateDir}}/services
          Environment=GLOBULAR_BOOTSTRAP=1
          ExecStartPre=/bin/sh -c 'mkdir -p {{.StateDir}}/catalog && chown globular:globular {{.StateDir}}/catalog'
          ExecStartPre=/bin/sh -c 'for i in $(seq 1 60); do [ -f /var/lib/globular/pki/issued/services/service.crt ] && exit 0; sleep 1; done; echo "TLS cert not ready"; exit 1'
          ExecStart={{.Prefix}}/bin/catalog_server
          Restart=on-failure
          RestartSec=2
          LimitNOFILE=524288

          [Install]
          WantedBy=multi-user.target

  - id: enable-catalog
    type: enable_services
    services:
      - globular-catalog.service

  - id: start-catalog
    type: start_services
    services:
      - globular-catalog.service
    binaries:
      globular-catalog.service: catalog_server

  - id: health-catalog
    type: health_checks
    services:
      - globular-catalog.service
