aliases:
  infra.desired_hash_consistency:
    - desired hash mismatch
    - checksum mismatch
    - drift loop
    - install loop
    - raw artifact digest
    - ComputeInfrastructureDesiredHash
    - ResolvedArtifactDigest

  service.restart_singleflight:
    - restart storm
    - SIGTERM storm
    - start-limit-hit
    - repeated restart
    - restart loop

  infra.heartbeat_not_desired_authority:
    - heartbeat created release
    - ManagedInstalled created InfrastructureRelease
    - runtime promoted to desired

  etcd.nospace_alarm:
    - etcd NOSPACE
    - etcd disk full
    - etcd write blocked

  objectstore.availability_degraded:
    - minio offline
    - minio disk offline
    - erasure coding degraded

  critical_state.absence_is_not_destructive_intent:
    - missing key stopped service
    - absent config disabled runtime
    - keepalived disabled by absent spec
    - etcd key missing stopped service
    - absence treated as disable intent

  runtime.installed_state_must_match_package_kind:
    - command package flagged as missing unit
    - command package flagged as missing systemd unit
    - command package flagged
    - installed_state_runtime_mismatch
    - COMMAND package missing systemd unit
    - doctor false positive command package
    - package kind mismatch installed state
    - flagged as missing systemd unit

  desired.bootstrap_state_requires_promotion:
    - bootstrap state treated as authoritative
    - first boot claimed convergence
    - bootstrap discovery promoted to desired
    - bootstrap state requires promotion
