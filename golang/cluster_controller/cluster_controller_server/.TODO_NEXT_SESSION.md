# Next Session TODO

## Immediate Fixes Needed

### 1. ~~Wired-first IP selection (cross-cutting)~~ DONE
- ✅ `gatherIPs()` in node-agent sorts wired (eth*/eno*/enp*/ens*/enx*) before WiFi
- ✅ `nodeRoutableIP()` in controller picks first from pre-sorted list
- ✅ `nodeAnyIPIsEtcdMember()` helper checks ALL IPs
- TODO: Join script `hostname -I | awk` — same preference (manual fix on nodes)

### 2. ~~Doctor rule for IP mismatch~~ DONE
- ✅ `node.multi_ip` rule in cluster_doctor detects:
  - Multiple routable IPs (wired+WiFi risk, elevated to WARN for etcd nodes)
  - Single-IP etcd nodes (INFO: verify wired connection)

### 3. ~~Network plan dispatch for joining nodes~~ DONE
- ✅ Root cause: `infraOnly` nodes skipped services reconciliation entirely
- ✅ Fix: filter desired services to infra-tier only instead of skipping
- ✅ Now xds/envoy/gateway/dns/discovery get installed during bootstrap

### 4. ~~etcd join phase oscillation~~ DONE
- ✅ Verified case now uses `nodeAnyIPIsEtcdMember()` — checks ALL IPs
- ✅ Started case also checks ALL IPs via `memberIsHealthy` loop

### 5. ~~Disable controller MemberAdd for safety~~ DONE
- ✅ Controller is now observe-only for etcd membership
- ✅ MemberAdd code path removed from reconcileEtcdJoinPhases
- ✅ Join script handles MemberAdd directly (critical for 1→2 expansion)
- ✅ Backward-compat: member_added/started cases retained for in-flight joins

## Current State
- 2-node etcd cluster running (globule-ryzen + globule-nuc)
- All TODO items complete
- Multi-IP fixes deployed (wired-first sorting, oscillation fix, doctor rule)
- Services reconciliation now works during bootstrap (infra-tier filtering)
- Controller is observe-only for etcd membership
