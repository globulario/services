# Globular V1.1 — High Availability & AI Routing

## Status: Not Started

Prerequisites: V1 complete, multi-node cluster operational.

---

## 1. Multi-Node HA

- [ ] 2+ node cluster running and healthy
- [ ] Services distributed across nodes
- [ ] Node failure triggers automatic redistribution
- [ ] etcd cluster (3-node) for quorum

---

## 2. AI Router Load Balancing

- [ ] ai_router in ACTIVE mode (currently NEUTRAL)
- [ ] Fix ai_router working directory (same issue as echo — status 200/CHDIR)
- [ ] Dynamic endpoint weights computed from metrics
- [ ] Circuit breaker overrides applied to Envoy xDS
- [ ] Drain strategy works (endpoint graceful removal)
- [ ] Verify ai_router ↔ Envoy xDS integration end-to-end

---

## 3. AI Router Anomaly Detection

- [ ] Anomaly scoring from traffic patterns
- [ ] Automatic drain of unhealthy endpoints
- [ ] Learning store records routing decisions to ai_memory

---

## 4. C# Runtime & Service Generation (after HA)

- [ ] Finish Globular.Runtime.DotNet (Hosting, Authorization, Validation)
- [ ] Finish CatalogService.Host example (EF Core persistence)
- [ ] `globular-cli generate service --proto <file> --lang csharp --out <dir>`
- [ ] `globular-cli generate service --proto <file> --lang go --out <dir>`
- [ ] EF Core persistence generator from proto definitions
- [ ] Package and deploy C# services via the same pkg publish pipeline
- [ ] Validate C# service registers with gateway, TLS, RBAC interceptors

---

## 5. Items Deferred from V1

- RBAC externalization phases 3b-6
- Execution governor phases 2-7E
- Dependency graphs and impact analysis
- C# runtime parity
- Email/Slack notifiers
- Data exfiltration detection
- Botnet/coordinated attack correlation
- Claude CLI as infrastructure package (download at install, distribute auth token as cluster secret)
- Claude CLI auth per-node automation (currently manual `claude /login`)
- ai_executor restart-service verification (check specific service, not any healthy node)
