---
name: globular-operator
description: Operate and diagnose a running Globular cluster — health/doctor findings, node join/remove, package deploy via repository, DNS/ACME, PKI rotation, MinIO/object-store topology, etcd/keepalived recovery, day-0/1/2 runbooks, and incident triage. Use when the task is RUNNING the cluster (observe → diagnose → act → verify), not editing service source (that's sensei-architect). Routes to the operational-knowledge corpus and the matching typed MCP tools; pulls only the relevant slice on demand.
---

<!-- GENERATED by scripts/gen-operator-skill.py from docs/operational-knowledge/ — do not edit by hand; re-run the generator. -->

# Globular Operator

Act as the cluster's operator: observe first, act through typed APIs, verify, and leave an audit trail.

This skill does **not** duplicate the operational-knowledge corpus — it is a **loop + a router**. The facts
live in `docs/operational-knowledge/` (the signed day-0 seed that also feeds ai-memory). This skill tells you
*which slice to Read for the task in front of you* and *which typed tool pairs with it*, so you load depth
only when it's relevant.

Scope boundary: **operating** the cluster (this skill) vs **changing service code** (`sensei-architect`). If the
task is a code edit in a high-risk dir, stop and use sensei-architect instead.

## The operate loop (always)

`OBSERVE → DIAGNOSE → RECOMMEND → [APPROVE] → EXECUTE → VERIFY`

1. **Observe before acting.** Never prescribe before you diagnose. Pull live state with the MCP tools, not from memory.
2. **Walk the 4 layers, in order.** Repository → Desired → Installed → Runtime. Never assume Desired == Installed == Running. Most "mystery" findings are a layer disagreement.
3. **Never invent state.** Reason only from observable, verifiable evidence. A recalled fact (or a seed entry) is a hypothesis until a live tool confirms it. Absence of evidence is not evidence of failure — check the snapshot's completeness markers.
4. **Typed actions only.** Mutate through typed gRPC/MCP calls and workflows, never ad-hoc shell against production. If there is no typed API for an action, it should not be done by an agent.
5. **Three-tier permissions.** Tier 0 (observe) is always safe. Tier 1 (restart, clear cache) is pre-approved. Tier 2 (destructive: wipe, remove-node, topology change, PKI rollback) **requires explicit human approval** — surface the exact command and wait.
6. **Audit + fail-safe.** Every action leaves a durable record. If AI services are down, the cluster converges deterministically without you — AI is supplementary, never required.

Enforcement note: the non-negotiable HARD RULES (etcd is sole authority, no localhost for remote, mTLS everywhere,
no secrets in etcd/source, security boundaries) live in **CLAUDE.md** and are hook-enforced. This skill assumes
them; it does not restate them.

## Router — topical quick-reference

Read paths on demand; do not preload the corpus. Tools are the typed MCP calls that pair with each topic.

### Diagnosis & health
tools: `cluster_get_doctor_report`, `cluster_explain_finding`, `cluster_get_health`, `infra_probe_all`, `infra_explain_stall`

- **Service role — cluster-doctor** — What cluster-doctor IS (the operational awareness layer), what it OWNS (findings, severity, · `docs/operational-knowledge/service-roles/cluster-doctor.yaml`
- **Fixing cluster-doctor ERROR and WARN findings — root causes and remediation** — Root-cause analysis and durable fixes for the most common cluster-doctor findings · `docs/operational-knowledge/runbooks/fix-doctor-error-findings.yaml`
- **Runbook — cluster-doctor MinIO CRITICAL false positives on DataIncomplete snapshots** — When the cluster-doctor snapshot collector suffers transient RPC or etcd read errors, · `docs/operational-knowledge/runbooks/doctor-minio-data-incomplete-false-positives.yaml`
- **Day-2 — Routine cluster operation** — What "normal" looks like, what cadences matter, and how to keep the cluster healthy over · `docs/operational-knowledge/stages/day-2-maintenance.yaml`

### Node lifecycle (join / remove / rejoin)
tools: `cluster_list_nodes`, `cluster_remove_node`, `cluster_get_node_full_status`

- **Day-1 — Multi-node join and capacity posture** — What it takes to grow from a single day-0 node to a larger cluster. Covers the join · `docs/operational-knowledge/stages/day-1-join.yaml`
- **Node Removal** — Removing a node from a Globular cluster requires two phases: cluster-side deregistration and local cleanup on the node itself. Doing only o… · `docs/operational-knowledge/node-removal.md`
- **Incident — node remove/rejoin drill cascade: Scylla group0 quorum loss + repository index split (2026-07-09)** — Status: open — durability restored on the live cluster; 5 scars filed for lawful repair (code + tests + contracts). No fixes applied yet · `docs/operational-knowledge/incidents/node-rejoin-scylla-group0-repository-2026-07-09.md`

### Packages & releases
tools: `package_build`, `package_publish`, `repository_active_release`, `repository_verify_artifact`, `cluster_get_convergence_detail`

- **How to Deploy a Package — MCP Tool Playbook** — This document is the canonical procedure for deploying or updating a Globular service package using MCP tools. It is written for AI agents… · `docs/operational-knowledge/deploy-package-via-mcp.md`
- **Globular Package System — Definitive Reference** — This document is the canonical reference for what a Globular package is, what files it must contain, how those files are validated, and how… · `docs/operational-knowledge/packages.md`
- **Package system — anatomy, identity, install lifecycle, validation** — The full operational picture of Globular packages: what a package IS, the files · `docs/operational-knowledge/stages/package-system.yaml`
- **Installed artifact system — filesystem layout, authority, and permissions** — Canonical runtime artifact map for Globular nodes: where binaries live, where · `docs/operational-knowledge/stages/installed-artifact-system.yaml`
- **Day-1 — Repository, package, and deploy pipeline** — How code reaches running services across the cluster. Covers the repository service, the · `docs/operational-knowledge/stages/day-1-deploy-pipeline.yaml`
- **Runbook — Deploy a controller code fix via the repository pipeline** — Concrete step-by-step procedure to take a code change in `golang/cluster_controller/`, · `docs/operational-knowledge/runbooks/deploy-controller-fix-via-repository.yaml`
- **Service version management — generated version contract** — Canonical rules for service runtime version identity, centered on · `docs/operational-knowledge/stages/service-version-management.yaml`
- **Profile system — intent, invariants, normalization, enforcement** — Canonical profile semantics for Globular: what profiles mean, who assigns them, · `docs/operational-knowledge/stages/profile-system.yaml`

### Storage / MinIO / disk
tools: `backup_list_minio_buckets`

- **Runbook — Add a node to an existing MinIO pool** — Phased procedure to expand an existing distributed MinIO pool with one additional node · `docs/operational-knowledge/runbooks/add-node-to-minio-pool.yaml`
- **Runbook — Safe MinIO restart when topology has changed** — A MinIO restart on a node whose drive-count argv disagrees with the · `docs/operational-knowledge/runbooks/safe-minio-topology-restart.yaml`
- **Runbook — Recover from a stuck objectstore topology apply** — Historical: the objectstore-apply-watcher silently died when its etcd watch returned · `docs/operational-knowledge/runbooks/recover-stuck-topology-apply.yaml`
- **Runbook — Detecting and removing a stale destructive MinIO topology proposal** — The topology planning process generates a TopologyProposal record in etcd when the · `docs/operational-knowledge/runbooks/minio-stale-topology-proposal.yaml`
- **Runbook — Re-publish artifact data after a MinIO pool topology change** — When the MinIO topology changes (standalone → distributed, or pool growth · `docs/operational-knowledge/runbooks/republish-artifact-data-after-pool-change.yaml`
- **Runbook — Repartition a shared disk for MinIO data** — Safe, in-place procedure to shrink an NTFS partition (Windows data) and create a new ext4 · `docs/operational-knowledge/runbooks/repartition-shared-disk.yaml`
- **Runbook — Clean ghost loop devices** — During Day-0/Day-1 testing on nodes without dedicated MinIO disks, operators · `docs/operational-knowledge/runbooks/clean-ghost-loop-devices.yaml`
- **Day-1 — Object store (MinIO) topology** — Knowledge needed to operate, expand, or recover the MinIO object store after day-1 is reached · `docs/operational-knowledge/stages/day-1-objectstore.yaml`

### etcd / ingress / DNS / networking
- **Runbook — Recover from an evicted or stale etcd member** — A healthy etcd member was evicted (often due to VIP-contamination or a removed node) · `docs/operational-knowledge/runbooks/recover-etcd-member-eviction.yaml`
- **Runbook — Recover from a lost or flapping keepalived VIP** — The cluster VIP is missing (no node holds it) or flapping between nodes. External · `docs/operational-knowledge/runbooks/recover-keepalived-vip-loss.yaml`
- **Day-1 — keepalived (VIP) enable** — What changes when keepalived is enabled, the floating-VIP semantics, and the entire class · `docs/operational-knowledge/stages/day-1-keepalived.yaml`
- **Service role — ingress** — End-to-end ingress contract: external domain exposure, TLS termination, · `docs/operational-knowledge/service-roles/ingress.yaml`
- **Managing DNS records in Globular** — Globular runs its own authoritative DNS service (dnsserver, port 10006 gRPC, port 53 UDP/TCP). Public-facing zones like globular.io resolve… · `docs/operational-knowledge/dns-records.md`
- **Service role — dns** — What dns IS, what it OWNS, when it MUST run, and where DNS must NOT be · `docs/operational-knowledge/service-roles/dns.yaml`
- **Runbook — Set up a public domain with ACME TLS certificate and landing page** — Step-by-step procedure to expose a Globular cluster under a public FQDN (e.g. globular.io), · `docs/operational-knowledge/runbooks/setup-public-domain-acme.yaml`
- **Day-1 — Public domain, ACME certificates, and external webroot** — Knowledge needed to expose a Globular cluster under a public domain name (e.g. globular.io), · `docs/operational-knowledge/stages/day-1-public-domain.yaml`
- **Runbook — Configure Google Workspace mail for a Globular-served zone** — End-to-end procedure for making a Globular-managed zone (e.g. globular.io) accept · `docs/operational-knowledge/runbooks/configure-google-workspace-mail.yaml`

### Security / PKI
- **Runbook — Rotate PKI certificates (CA and per-service)** — Two distinct rotations: per-service certs (auto-handled, but documented for visibility) · `docs/operational-knowledge/runbooks/rotate-pki-certificates.yaml`
- **Security system — token, certificate, and key policy** — Canonical cluster security policy for credential types and lifecycle boundaries: · `docs/operational-knowledge/stages/security-system.yaml`
- **Incident closure — scylla-manager TLS trust (2026-06-03)** — Status: closed · `docs/operational-knowledge/incidents/scylla-manager-tls-trust-2026-06-03.md`

### Bootstrap / upgrade / backup
tools: `backup_restore_plan`, `backup_preflight_check`, `backup_get_recovery_posture`

- **Runbook — Recover from a failed day-0 bootstrap** — Day-0 bootstrap can fail at several distinct points. This runbook walks through diagnosing · `docs/operational-knowledge/runbooks/recover-day-0-bootstrap-failure.yaml`
- **Day-0 — Initial cluster bootstrap** — What is true about a cluster immediately after the Day-0 install script (`install-day0.sh`) · `docs/operational-knowledge/stages/day-0-bootstrap.yaml`
- **Runbook — Recover from a failed platform upgrade** — `globular platform-upgrade vX.Y.Z` advances the active release BOM and triggers · `docs/operational-knowledge/runbooks/recover-failed-platform-upgrade.yaml`
- **Runbook — Restore cluster state from backup** — Disaster recovery for catastrophic loss of one or more truth-bearing data · `docs/operational-knowledge/runbooks/restore-from-backup.yaml`

### Awareness graph (AWG / sensei) operations
- **AWG Operator Guide — using the Awareness Graph as an AI operator** — This is the canonical, human- and AI-readable reference for using AWG · `docs/operational-knowledge/awg-operator-guide.md`
- **Service role — awareness-graph (AWG standalone sidecar)** — What AWG (the Awareness Graph) IS after its extraction from Globular: a standalone · `docs/operational-knowledge/service-roles/awareness-graph.yaml`
- **Awareness-graph — initialization, population, and cluster refresh** — How to build, load, and refresh the awareness graph. Covers the full build pipeline · `docs/operational-knowledge/stages/awareness-graph-operations.yaml`
- **Runbook — Initialize or rebuild the awareness graph** — AWG is a standalone sidecar: `awg serve` runs the gRPC server plus a managed Oxigraph · `docs/operational-knowledge/runbooks/initialize-awareness-graph.yaml`

### Prior scars & learned patterns (read before repeating diagnosis)
- **Milestone — cluster at zero findings (2026-06-03)** — Status: PASS · `docs/operational-knowledge/incidents/cluster-zero-findings-2026-06-03.md`
- **Incident closure — sidecar receipt retirement (2026-06-03)** — Status: closed · `docs/operational-knowledge/incidents/sidecar-receipt-retirement-2026-06-03.md`
- **Incident closure — scylla-manager TLS trust (2026-06-03)** — Status: closed · `docs/operational-knowledge/incidents/scylla-manager-tls-trust-2026-06-03.md`
- **Incident — node remove/rejoin drill cascade: Scylla group0 quorum loss + repository index split (2026-07-09)** — Status: open — durability restored on the live cluster; 5 scars filed for lawful repair (code + tests + contracts). No fixes applied yet · `docs/operational-knowledge/incidents/node-rejoin-scylla-group0-repository-2026-07-09.md`
- **Learned lessons — promoted from runtime ai-memory (2026-06)** — Durable lessons that graduated from runtime ai-memory into the Day-0 seed so they · `docs/operational-knowledge/stages/learned-2026-06.yaml`

## Full index — every corpus entry

Auto-enumerated from `docs/operational-knowledge/`; complete by construction.

### Canonical guides
- **AWG Operator Guide — using the Awareness Graph as an AI operator** — This is the canonical, human- and AI-readable reference for using AWG · `docs/operational-knowledge/awg-operator-guide.md`
- **How to Deploy a Package — MCP Tool Playbook** — This document is the canonical procedure for deploying or updating a Globular service package using MCP tools. It is written for AI agents… · `docs/operational-knowledge/deploy-package-via-mcp.md`
- **Managing DNS records in Globular** — Globular runs its own authoritative DNS service (dnsserver, port 10006 gRPC, port 53 UDP/TCP). Public-facing zones like globular.io resolve… · `docs/operational-knowledge/dns-records.md`
- **Node Removal** — Removing a node from a Globular cluster requires two phases: cluster-side deregistration and local cleanup on the node itself. Doing only o… · `docs/operational-knowledge/node-removal.md`
- **Globular Package System — Definitive Reference** — This document is the canonical reference for what a Globular package is, what files it must contain, how those files are validated, and how… · `docs/operational-knowledge/packages.md`

### Lifecycle stages (`stages/`)
- **Awareness-graph — initialization, population, and cluster refresh** — How to build, load, and refresh the awareness graph. Covers the full build pipeline · `docs/operational-knowledge/stages/awareness-graph-operations.yaml`
- **Day-0 — Initial cluster bootstrap** — What is true about a cluster immediately after the Day-0 install script (`install-day0.sh`) · `docs/operational-knowledge/stages/day-0-bootstrap.yaml`
- **Day-1 — Repository, package, and deploy pipeline** — How code reaches running services across the cluster. Covers the repository service, the · `docs/operational-knowledge/stages/day-1-deploy-pipeline.yaml`
- **Day-1 — Multi-node join and capacity posture** — What it takes to grow from a single day-0 node to a larger cluster. Covers the join · `docs/operational-knowledge/stages/day-1-join.yaml`
- **Day-1 — keepalived (VIP) enable** — What changes when keepalived is enabled, the floating-VIP semantics, and the entire class · `docs/operational-knowledge/stages/day-1-keepalived.yaml`
- **Day-1 — Object store (MinIO) topology** — Knowledge needed to operate, expand, or recover the MinIO object store after day-1 is reached · `docs/operational-knowledge/stages/day-1-objectstore.yaml`
- **Day-1 — Public domain, ACME certificates, and external webroot** — Knowledge needed to expose a Globular cluster under a public domain name (e.g. globular.io), · `docs/operational-knowledge/stages/day-1-public-domain.yaml`
- **Day-2 — Routine cluster operation** — What "normal" looks like, what cadences matter, and how to keep the cluster healthy over · `docs/operational-knowledge/stages/day-2-maintenance.yaml`
- **Service backbone — gRPC service/client/interceptor architecture** — Canonical blueprint for how Globular services run and collaborate over gRPC: · `docs/operational-knowledge/stages/grpc-service-backbone.yaml`
- **Installed artifact system — filesystem layout, authority, and permissions** — Canonical runtime artifact map for Globular nodes: where binaries live, where · `docs/operational-knowledge/stages/installed-artifact-system.yaml`
- **Learned lessons — promoted from runtime ai-memory (2026-06)** — Durable lessons that graduated from runtime ai-memory into the Day-0 seed so they · `docs/operational-knowledge/stages/learned-2026-06.yaml`
- **Package system — anatomy, identity, install lifecycle, validation** — The full operational picture of Globular packages: what a package IS, the files · `docs/operational-knowledge/stages/package-system.yaml`
- **Profile system — intent, invariants, normalization, enforcement** — Canonical profile semantics for Globular: what profiles mean, who assigns them, · `docs/operational-knowledge/stages/profile-system.yaml`
- **Security system — token, certificate, and key policy** — Canonical cluster security policy for credential types and lifecycle boundaries: · `docs/operational-knowledge/stages/security-system.yaml`
- **Service version management — generated version contract** — Canonical rules for service runtime version identity, centered on · `docs/operational-knowledge/stages/service-version-management.yaml`

### Runbooks (`runbooks/`)
- **Runbook — Add a node to an existing MinIO pool** — Phased procedure to expand an existing distributed MinIO pool with one additional node · `docs/operational-knowledge/runbooks/add-node-to-minio-pool.yaml`
- **Runbook — Clean ghost loop devices** — During Day-0/Day-1 testing on nodes without dedicated MinIO disks, operators · `docs/operational-knowledge/runbooks/clean-ghost-loop-devices.yaml`
- **Runbook — Configure Google Workspace mail for a Globular-served zone** — End-to-end procedure for making a Globular-managed zone (e.g. globular.io) accept · `docs/operational-knowledge/runbooks/configure-google-workspace-mail.yaml`
- **Runbook — Deploy a controller code fix via the repository pipeline** — Concrete step-by-step procedure to take a code change in `golang/cluster_controller/`, · `docs/operational-knowledge/runbooks/deploy-controller-fix-via-repository.yaml`
- **Runbook — cluster-doctor MinIO CRITICAL false positives on DataIncomplete snapshots** — When the cluster-doctor snapshot collector suffers transient RPC or etcd read errors, · `docs/operational-knowledge/runbooks/doctor-minio-data-incomplete-false-positives.yaml`
- **Fixing cluster-doctor ERROR and WARN findings — root causes and remediation** — Root-cause analysis and durable fixes for the most common cluster-doctor findings · `docs/operational-knowledge/runbooks/fix-doctor-error-findings.yaml`
- **Runbook — Initialize or rebuild the awareness graph** — AWG is a standalone sidecar: `awg serve` runs the gRPC server plus a managed Oxigraph · `docs/operational-knowledge/runbooks/initialize-awareness-graph.yaml`
- **Runbook — Detecting and removing a stale destructive MinIO topology proposal** — The topology planning process generates a TopologyProposal record in etcd when the · `docs/operational-knowledge/runbooks/minio-stale-topology-proposal.yaml`
- **Runbook — Recover from a failed day-0 bootstrap** — Day-0 bootstrap can fail at several distinct points. This runbook walks through diagnosing · `docs/operational-knowledge/runbooks/recover-day-0-bootstrap-failure.yaml`
- **Runbook — Recover from an evicted or stale etcd member** — A healthy etcd member was evicted (often due to VIP-contamination or a removed node) · `docs/operational-knowledge/runbooks/recover-etcd-member-eviction.yaml`
- **Runbook — Recover from a failed platform upgrade** — `globular platform-upgrade vX.Y.Z` advances the active release BOM and triggers · `docs/operational-knowledge/runbooks/recover-failed-platform-upgrade.yaml`
- **Runbook — Recover from a lost or flapping keepalived VIP** — The cluster VIP is missing (no node holds it) or flapping between nodes. External · `docs/operational-knowledge/runbooks/recover-keepalived-vip-loss.yaml`
- **Runbook — Recover from a stuck objectstore topology apply** — Historical: the objectstore-apply-watcher silently died when its etcd watch returned · `docs/operational-knowledge/runbooks/recover-stuck-topology-apply.yaml`
- **Runbook — Repartition a shared disk for MinIO data** — Safe, in-place procedure to shrink an NTFS partition (Windows data) and create a new ext4 · `docs/operational-knowledge/runbooks/repartition-shared-disk.yaml`
- **Runbook — Re-publish artifact data after a MinIO pool topology change** — When the MinIO topology changes (standalone → distributed, or pool growth · `docs/operational-knowledge/runbooks/republish-artifact-data-after-pool-change.yaml`
- **Runbook — Restore cluster state from backup** — Disaster recovery for catastrophic loss of one or more truth-bearing data · `docs/operational-knowledge/runbooks/restore-from-backup.yaml`
- **Runbook — Rotate PKI certificates (CA and per-service)** — Two distinct rotations: per-service certs (auto-handled, but documented for visibility) · `docs/operational-knowledge/runbooks/rotate-pki-certificates.yaml`
- **Runbook — Safe MinIO restart when topology has changed** — A MinIO restart on a node whose drive-count argv disagrees with the · `docs/operational-knowledge/runbooks/safe-minio-topology-restart.yaml`
- **Runbook — Set up a public domain with ACME TLS certificate and landing page** — Step-by-step procedure to expose a Globular cluster under a public FQDN (e.g. globular.io), · `docs/operational-knowledge/runbooks/setup-public-domain-acme.yaml`

### Service roles (`service-roles/`)
- **Service role — ai-executor** — What ai-executor IS, the three-tier permission model, the typed-actions-only rule, and · `docs/operational-knowledge/service-roles/ai-executor.yaml`
- **Service role — ai-memory** — What ai-memory IS, the schema, the seed-vs-organic distinction, and the immutability · `docs/operational-knowledge/service-roles/ai-memory.yaml`
- **Service role — ai-router** — What ai-router IS, what it routes, and how it integrates with the AI tier · `docs/operational-knowledge/service-roles/ai-router.yaml`
- **Service role — ai-watcher** — What ai-watcher IS, what events it subscribes to, and how it bridges raw cluster events · `docs/operational-knowledge/service-roles/ai-watcher.yaml`
- **Service role — awareness-graph (AWG standalone sidecar)** — What AWG (the Awareness Graph) IS after its extraction from Globular: a standalone · `docs/operational-knowledge/service-roles/awareness-graph.yaml`
- **Service role — cluster-controller** — What cluster-controller IS, what it OWNS, what it MUST NOT do, and how to recognize it · `docs/operational-knowledge/service-roles/cluster-controller.yaml`
- **Service role — cluster-doctor** — What cluster-doctor IS (the operational awareness layer), what it OWNS (findings, severity, · `docs/operational-knowledge/service-roles/cluster-doctor.yaml`
- **Service role — dns** — What dns IS, what it OWNS, when it MUST run, and where DNS must NOT be · `docs/operational-knowledge/service-roles/dns.yaml`
- **Service role — ingress** — End-to-end ingress contract: external domain exposure, TLS termination, · `docs/operational-knowledge/service-roles/ingress.yaml`
- **Service role — mcp** — What the MCP server IS, its untracked-service status and operational implications, · `docs/operational-knowledge/service-roles/mcp.yaml`
- **Service role — node-agent** — What node-agent IS, what it OWNS on each node, what it executes vs delegates upward, · `docs/operational-knowledge/service-roles/node-agent.yaml`
- **Service role — repository** — What the repository service IS, what it OWNS, the trust boundary between metadata (Scylla) · `docs/operational-knowledge/service-roles/repository.yaml`
- **Service role — workflow** — What the workflow service IS, the idempotency contract, and why every cluster mutation · `docs/operational-knowledge/service-roles/workflow.yaml`

### Incidents & learned patterns (`incidents/`)
- **Milestone — cluster at zero findings (2026-06-03)** — Status: PASS · `docs/operational-knowledge/incidents/cluster-zero-findings-2026-06-03.md`
- **Incident — node remove/rejoin drill cascade: Scylla group0 quorum loss + repository index split (2026-07-09)** — Status: open — durability restored on the live cluster; 5 scars filed for lawful repair (code + tests + contracts). No fixes applied yet · `docs/operational-knowledge/incidents/node-rejoin-scylla-group0-repository-2026-07-09.md`
- **Incident closure — scylla-manager TLS trust (2026-06-03)** — Status: closed · `docs/operational-knowledge/incidents/scylla-manager-tls-trust-2026-06-03.md`
- **Incident closure — sidecar receipt retirement (2026-06-03)** — Status: closed · `docs/operational-knowledge/incidents/sidecar-receipt-retirement-2026-06-03.md`

## After acting — close the loop

- Verify against live tools that the change took effect at the layer you targeted (don't declare success from a green summary alone).
- If the session produced a durable operational lesson (a new failure mode, a runbook gap, a corrected assumption), record it — write to **ai-memory** (`memory_store`, project `globular-services`), not flat files. The `docs/operational-knowledge/` corpus is the *seed* and is release-managed; runtime lessons go to ai-memory and are promoted to the corpus only through the build pipeline.
- Never use multi-word `text_search` on ai-memory (known bug — returns 0); query by `tags=`/`type=` or `memory_get` by id.

## Non-negotiables

- Observe before you prescribe; never fabricate state from memory.
- Tier-2 destructive actions need explicit human approval — present the exact command, wait.
- Typed actions only; no ad-hoc shell mutations against production.
- This skill is know-how, not enforcement — the HARD RULES in CLAUDE.md still bind, and this skill never overrides them.
