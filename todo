Good. Do not add new fixes yet.

Next task is validation of the hardening changes under stress.

Run focused tests for these scenarios and report exact behavior:

1. Restart storm test

   * Trigger 4 concurrent workflows that would call maybe_restart/restart on the same service/node
   * Verify only one actual restart occurs
   * Verify waiters return cleanly
   * Verify no stuck dedup entry remains after completion
   * Verify no systemd start-limit-hit

2. Controller restart during active workflow

   * Start deploy
   * Restart controller mid-flight
   * Verify completed runs are not resumed incorrectly
   * Verify no bogus FAILED transition
   * Verify reconciliation continues

3. Controller endpoint resolution

   * On a non-colocated node, verify cluster-doctor resolves controller from etcd
   * Verify leader change updates resolution correctly
   * Verify fallback path only occurs when etcd is unavailable
   * Log when fallback is used

4. Version metadata validation

   * Verify cluster-controller, cluster-doctor, node-agent, workflow, and other core services report the correct runtime version/build
   * Show evidence from logs or status APIs

5. workflow_engine_error retry behavior

   * Simulate a transient engine-side error
   * Verify release stays retryable and is reconciled successfully later
   * Verify real business failure still becomes FAILED

For each test, provide:

* commands run
* observed logs
* before/after behavior
* pass/fail
* any remaining gap

Do not implement more code unless a test reveals a real defect.
