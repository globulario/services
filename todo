curl -s -X POST http://127.0.0.1:10050/mcp -H 'Content-Type: application/json' -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' 2>/dev/null | python3 -c "import json,sys; [print(t['name']) for t in json.loads(sys.stdin.readline()).get('result',{}).get('tools',[]) if 'memory' in t['name'] or 'session' in t['name']]"
                                                                                                                      
memory_store
memory_query
memory_get
memory_update
memory_delete
memory_list
session_save
session_resume
