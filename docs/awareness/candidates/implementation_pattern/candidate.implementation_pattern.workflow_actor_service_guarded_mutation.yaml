# GENERATED candidate by `awg bootstrap` — status: candidate.
# A suggestion for human review; never auto-promoted. To accept,
# move it under docs/awareness/architecture/ and set status accordingly.
id: candidate.implementation_pattern.workflow_actor_service_guarded_mutation
class: ImplementationPattern
label: WorkflowActorService exposes a mutation surface (candidate)
status: candidate
confidence: candidate
description: WorkflowActorService has write RPCs (read_or_write=write). A mutation surface should be validated/auditable — review whether it follows pattern.guarded_mutation_flow (a GuardedMutationFlow).
source_files:
  - proto/workflow.proto
