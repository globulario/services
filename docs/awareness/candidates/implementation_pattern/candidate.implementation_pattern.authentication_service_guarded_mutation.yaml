# GENERATED candidate by `awg bootstrap` — status: candidate.
# A suggestion for human review; never auto-promoted. To accept,
# move it under docs/awareness/architecture/ and set status accordingly.
id: candidate.implementation_pattern.authentication_service_guarded_mutation
class: ImplementationPattern
label: AuthenticationService exposes a mutation surface (candidate)
status: candidate
confidence: candidate
description: AuthenticationService has write RPCs (read_or_write=read_write). A mutation surface should be validated/auditable — review whether it follows pattern.guarded_mutation_flow (a GuardedMutationFlow).
source_files:
  - proto/authentication.proto
