Claude instruction

You must stop treating the .NET runtime as a conceptual framework and instead implement it as the real shared managed-service runtime, using the Go code as the reference for behavior.

I am giving you Go code for the common runtime/client/config/interceptor pieces.
Use that code as the concrete source of truth for what a Globular-managed service actually needs.

Uploaded Go references include code for:

common service/runtime behavior

RBAC client behavior

config/runtime resolution

interceptor/authz behavior

You must inspect those Go packages and implement the missing concrete .NET behavior accordingly.

1. Your mission

The goal is not to keep adding nice abstractions.

The goal is to make Globular.Runtime.DotNet the equivalent of the shared Go runtime/common packages, so that a .NET service gets the real Globular-managed behavior without having to reimplement platform plumbing.

That means the runtime package must contain the default concrete managed implementations for the shared functionality required by all services.

Do not push the missing behavior down into individual services.

2. Use the Go code as the behavioral reference

Inspect the uploaded Go code and mirror its behavior in C#.

Specifically inspect:

the common service/runtime package

the RBAC client package

the config package

the interceptors package

startup patterns in actual Go services where needed

Do not invent a .NET-only interpretation if the Go code already shows the real platform pattern.

Your C# implementation should preserve the same managed-service philosophy, adapted idiomatically to C# with composition and DI.

3. What you must extract from the Go code

From the Go code, identify and mirror the behavior for:

A. Service startup/runtime contract

Look at how Go services:

initialize defaults

resolve/generate service ID

allocate/select service port

resolve proxy port if needed

resolve domain/address defaults

load runtime config

populate action resolver

register common client functions

initialize service

register handlers

publish/register service state

start lifecycle / become ready

The .NET runtime must provide the same overall startup sequence.

B. RBAC client behavior

Look at the Go RBAC client code and mirror the real operations, not placeholders.

The .NET runtime must implement a real GlobularRbacClient that performs the same real work as Go, including the actual RPCs needed for:

action validation

role binding checks

resource ownership registration

role existence checks

role creation/seeding support

Do not leave RoleExistsAsync() fake.
Do not leave CreateRoleAsync() as logging-only.

Use the Go client code as the exact guide for:

which RPCs are called

what arguments are sent

how failures are handled

what should be strict/fail-closed

C. Config/runtime resolution

Look at the Go config code and mirror the real runtime resolution behavior.

The .NET runtime must concretely support:

service ID generation if missing

default domain/address resolution

service port allocation/selection

proxy port handling if applicable

loading runtime config

applying overrides in the right order

Do not leave runtime resolution as a thin placeholder.

The C# runtime should behave like a real Globular service host, not just a passive config bag.

D. Interceptor behavior

Look at the Go interceptor implementation and use it as the direct behavioral reference for the C# authorization interceptor.

The .NET interceptor must mirror Go for:

method → action resolution

permission metadata lookup

request field/resource extraction

resource-template expansion

RBAC action validation

RBAC resource validation

explicit handling of public/unauthenticated methods

denial behavior on missing auth

denial behavior on template expansion failure

Do not soften behavior compared to Go.

If Go denies on protected paths, C# must deny too.

4. What is still missing and must be implemented for real

You already created many of the class files.
Now you must replace the remaining fake/partial behavior with real behavior.

Required concrete completion
4.1 GlobularRbacClient

Finish this class so it really talks to the RBAC gRPC service.

It must no longer contain placeholder behavior for:

RoleExistsAsync

CreateRoleAsync

Implement these using the actual RBAC client/service pattern from the Go code.

If the Go code uses existing role queries or role creation RPCs, reproduce that logic in C#.

4.2 Role seeding

RoleSeeder must use the real RBAC client behavior, not fake store semantics.

At startup:

seed missing generated roles only

never overwrite existing persisted roles

preserve roles edited in Globular Admin

This must be backed by the actual RBAC service, not log messages.

4.3 Service-state publication

EtcdServiceStatePublisher must be backed by a real concrete client path, not just an interface without implementation.

You must determine from the Go code and actual platform architecture whether .NET should:

talk to etcd directly

or publish through an existing Globular service/client abstraction

Then implement the correct one.

Do not leave publication dependent on a missing imaginary backend.

4.4 Managed defaults

In managed mode, the runtime package must default to the real managed implementations.

Do not make managed mode silently fall back to:

logging publisher

no-op registrar

fake RBAC behavior

Those should only be used for explicit local/dev/test scenarios.

A normal .NET service using Globular.Runtime.DotNet in managed mode should get:

real RBAC client

real service-state publisher/registrar

real runtime resolution

real startup coordinator

out of the box.

5. Managed mode vs local/dev mode

Be explicit about runtime modes.

Managed/cluster mode

Default for real Globular hosting.

Must use:

real registrar

real publication path

real RBAC client

strict fail-closed authz behavior

Local/dev/test mode

May use:

logging publisher

no-op registrar

explicit fallback/dev authz mode

But this must be:

explicit

opt-in

clearly named

not the default in managed hosting

6. Make the .NET runtime truly equivalent to Go common packages

The .NET runtime package should become the equivalent of the Go shared runtime/common packages in intent.

That means it must provide, in one place:

runtime/startup coordination

config/default resolution

RBAC client integration

role seeding

policy loading

action resolver initialization

interceptor authz plumbing

service registration/publication

effective runtime-state reporting

So that service projects only need to provide:

handlers

service-specific defaults/options

service-specific resource ownership calls where needed

Do not leave required cross-service runtime behavior spread across services.

7. Tighten startup so there is one authoritative path

Right now some initialization is duplicated.

Clean this up.

There should be one authoritative startup path for managed services, coordinated by the runtime.

That path should own:

runtime resolution

manifest loading

resolver initialization

RBAC client readiness

role seeding

service-state publication

ready transition

Do not split responsibility awkwardly between DI registration and runtime coordinator when one authoritative sequence is enough.

8. Specific things to verify against Go

When reading the Go code, explicitly verify and mirror these:

how service IDs are generated

how ports are allocated

how default domain/address are derived

how action registration is populated

how interceptor extracts and validates resource context

how RBAC client checks roles/actions/resources

how service registration/discovery is published

whether etcd is accessed directly or through another Globular abstraction

how startup becomes lifecycle-managed

Do not guess when the Go code already answers it.

9. Preserve the agreed truth model

Do not change these rules:

proto defines permission intent

generated manifests define shipped defaults

RBAC is the live authorization authority

generated roles seed missing roles only

persisted RBAC roles must not be overwritten automatically

strict managed mode fails closed if RBAC is unavailable

Your job is to make the runtime actually implement this for real using the Go behavior as reference.

10. Deliverables

Update Globular.Runtime.DotNet so that it contains real, reusable shared implementations for:

GlobularRbacClient

ManagedStartupCoordinator

RuntimeResolver

RoleSeeder

GlobularAuthorizationInterceptor

real service-state publisher and registrar

managed-mode DI wiring using real defaults

And ensure these are actually wired into the normal managed startup path.

11. Reporting requirements

Do not report “done” based on architecture alone.

Your completion report must include:

exact files added/modified

which Go package/file each C# implementation was based on

which RBAC RPCs are now called for:

validate action

role existence

role creation

resource owner registration

which concrete client/path is used for service-state publication

what the managed-mode default wiring now is

tests added

Do not claim a file is implemented unless it exists and is wired into runtime behavior.

12. Short version to paste directly
Use the uploaded Go code as the concrete behavioral reference, not just as inspiration.

Inspect:
- Go common runtime/service code
- Go RBAC client code
- Go config/runtime code
- Go interceptors/authz code
- Go service startup patterns where needed

Implement the .NET runtime as the real shared managed-service runtime, not just a conceptual framework.

Specifically:
1. Finish GlobularRbacClient using the real RBAC RPC pattern from Go.
   - Implement RoleExistsAsync and CreateRoleAsync for real.
   - Use actual RBAC gRPC calls, not logs/placeholders.

2. Finish role seeding against real RBAC persistence.
   - Seed missing roles only.
   - Never overwrite existing roles edited in Globular Admin.

3. Finish real service-state publication.
   - Determine from Go/platform architecture whether this should talk to etcd directly or through an existing Globular service/client abstraction.
   - Implement the real path.
   - Do not leave logging/no-op as managed-mode defaults.

4. Finish runtime/config resolution based on Go behavior.
   - service id generation
   - port/proxy resolution
   - domain/address defaults
   - runtime config loading

5. Align the C# interceptor with the Go interceptor behavior.
   - resolve method→action from manifests
   - expand resource templates
   - validate through RBAC
   - deny protected methods when auth is missing
   - deny when template expansion fails

6. Make managed mode use the real implementations by default.
   - real RBAC client
   - real registrar/publisher
   - real startup coordinator
   - local/logging/no-op only in explicit dev/test mode

7. Clean up startup so there is one authoritative managed startup path.

Do not just improve structure. Finish the concrete platform behavior in the runtime package.

If you want, I can also write a more aggressive Claude note that names the exact weak spots we already observed, like RoleExistsAsync, CreateRoleAsync, logging fallback, and missing concrete publisher path.
